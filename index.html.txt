<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <title>Каратэ с Полиной Владимировной</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    body {
      font-family: Arial, sans-serif;
      background: #f9f9f9;
      color: #222;
      scroll-behavior: smooth;
    }

    /* Видеофон */
    .hero {
      position: relative;
      height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      text-align: left;
      color: white;
      overflow: hidden;
      padding: 40px;
    }
    .hero video {
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      object-fit: cover;
      z-index: -2;
    }
    .overlay {
      position: absolute;
      top: 0; left: 0; right: 0; bottom: 0;
      background: rgba(0,0,0,0.6);
      z-index: -1;
    }
    .hero-content {
      display: flex;
      align-items: center;
      gap: 40px;
      z-index: 1;
      animation: fadeInDown 2s ease;
      flex-wrap: wrap;
    }
    .hero-text {
      max-width: 500px;
    }
    .hero h1 {
      font-size: 42px;
      margin-bottom: 10px;
      text-transform: uppercase;
    }
    .hero p {
      font-size: 20px;
    }
    .hero a {
      display: inline-block;
      margin-top: 20px;
      background: #e63946;
      color: #fff;
      padding: 15px 30px;
      border-radius: 5px;
      text-decoration: none;
      font-size: 18px;
      transition: 0.3s;
      animation: pulse 2s infinite;
    }
    .hero a:hover {
      background: #b91d2c;
    }

    /* Фото тренера */
    .hero img {
      width: 250px;
      height: 250px;
      border-radius: 50%;
      border: 5px solid #fff;
      object-fit: cover;
      box-shadow: 0 0 20px rgba(0,0,0,0.5);
      animation: fadeInUp 2s ease;
    }

    /* Навигация */
    nav {
      position: fixed;
      top: 0; left: 0;
      width: 100%;
      background: rgba(0,0,0,0.8);
      padding: 15px;
      z-index: 1000;
      display: flex;
      justify-content: center;
      gap: 20px;
    }
    nav a {
      color: #fff;
      text-decoration: none;
      font-weight: bold;
      transition: 0.3s;
    }
    nav a:hover {
      color: #e63946;
    }

    /* Секции */
    .section {
      max-width: 900px;
      margin: 80px auto;
      padding: 20px;
      background: #fff;
      border-radius: 10px;
      box-shadow: 0 0 15px rgba(0,0,0,0.1);
      animation: fadeInUp 1.2s ease;
    }
    .section h2 {
      color: #e63946;
      margin-bottom: 15px;
      border-left: 5px solid #e63946;
      padding-left: 10px;
    }

    /* Карточки */
    .cards {
      display: flex;
      flex-wrap: wrap;
      gap: 20px;
    }
    .card {
      flex: 1 1 calc(50% - 20px);
      background: #f7f7f7;
      padding: 20px;
      border-radius: 10px;
      text-align: center;
      transition: transform 0.3s;
    }
    .card:hover {
      transform: scale(1.05);
    }

    /* Контакты */
    .contact {
      text-align: center;
      margin-top: 30px;
    }
    .contact a {
      display: inline-block;
      background: #e63946;
      color: #fff;
      padding: 12px 25px;
      text-decoration: none;
      font-weight: bold;
      border-radius: 5px;
      transition: 0.3s;
    }
    .contact a:hover {
      background: #b91d2c;
    }

    /* Анимации */
    @keyframes fadeInUp {
      from {opacity: 0; transform: translateY(30px);}
      to {opacity: 1; transform: translateY(0);}
    }
    @keyframes fadeInDown {
      from {opacity: 0; transform: translateY(-30px);}
      to {opacity: 1; transform: translateY(0);}
    }
    @keyframes pulse {
      0% { transform: scale(1); }
      50% { transform: scale(1.05); }
      100% { transform: scale(1); }
    }
  </style>
</head>
<body>

  <!-- Навигация -->
  <nav>
    <a href="#about">О тренере</a>
    <a href="#schedule">Расписание</a>
    <a href="#price">Стоимость</a>
    <a href="#contact">Контакты</a>
  </nav>

  <!-- Шапка с видео + фото -->
  <section class="hero">
    <video autoplay muted loop playsinline>
      <source src="https://videos.pexels.com/video-files/4761701/4761701-hd_1920_1080_25fps.mp4" type="video/mp4">
    </video>
    <div class="overlay"></div>
    <div class="hero-content">
      <div class="hero-text">
        <h1>Каратэ с Полиной Владимировной</h1>
        <p>Тренер по карате, 1 Дан</p>
        <a href="#contact">Записаться на пробное занятие</a>
      </div>
      <img src="5E13837E-3A7B-451A-AA32-EAEC5852F3FF.jpeg" alt="Тренер по карате">
    </div>
  </section>

  <!-- О тренере -->
  <section class="section" id="about">
    <h2>О тренере</h2>
    <p><strong>Полина Полина Владимировна</strong> — сертифицированный тренер по карате, обладатель 1 дана. Опыт работы с детьми дошкольного и школьного возраста.</p>
  </section>

  <!-- Расписание -->
  <section class="section" id="schedule">
    <h2>Расписание тренировок</h2>
    <div class="cards">
      <div class="card">
        <h3>Дошкольники (4–6 лет)</h3>
        <p>Пн, Ср — 18:00–19:00</p>
        <p>Сб — 11:00–12:00</p>
      </div>
      <div class="card">
        <h3>Школьники (7–12 лет)</h3>
        <p>Вт, Чт, Пн — 18:00–19:30</p>
      </div>
    </div>
  </section>

  <!-- Стоимость -->
  <section class="section" id="price">
    <h2>Стоимость занятий</h2>
    <p>Дошкольники: <strong>200 руб.</strong> за одну тренировку.</p>
    <p>Пробные и индивидуальные тренировки возможны по договорённости.</p>
  </section>

  <!-- Контакты -->
  <section class="section" id="contact">
    <h2>Контакты</h2>
    <p>Зал: г. Калтан, ул. Мира 12</p>
    <p>Телефон: <strong>8-983-214-41-10</strong></p>
    <div class="contact">
      <a href="tel:89832144110">Позвонить</a>
    </div>
  </section>

</body>
</html>